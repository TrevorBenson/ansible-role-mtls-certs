---
# Molecule configuration for mTLS Certificate role testing
# Tests certificate generation across multiple OS versions

dependency:
  name: galaxy

driver:
  name: docker

platforms:
  # RHEL 9
  - name: ubi9-mtls-certs
    image: docker.io/cthullu/ubi9-ansible:latest
    pre_build_image: true
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    command: /sbin/init
    networks:
      - name: molecule
    groups:
      - ubi9

  # Rocky Linux 10
  - name: rocky10-mtls-certs
    image: docker.io/geerlingguy/docker-rockylinux10-ansible:latest
    pre_build_image: true
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    command: /sbin/init
    networks:
      - name: molecule
    groups:
      - rocky10


provisioner:
  name: ansible
  config_options:
    defaults:
      interpreter_python: auto_silent
      callbacks_enabled: profile_tasks, timer, yaml
      roles_path: "${MOLECULE_PROJECT_DIRECTORY}/..:~/.ansible/roles:/usr/share/ansible/roles:/etc/ansible/roles"
  inventory:
    host_vars:
      ubi9-mtls-certs:
        ansible_python_interpreter: /usr/bin/python3
      rocky10-mtls-certs:
        ansible_python_interpreter: /usr/bin/python3
  playbooks:
    converge: converge.yml
    verify: verify.yml

verifier:
  name: ansible

scenario:
  name: default
  test_sequence:
    - dependency
    - destroy
    - syntax
    - create
    - converge
    - idempotence
    - verify
    - destroy
